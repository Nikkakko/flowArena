import * as React from "react";

interface MobileMenuProps {}

const MobileMenu: React.FC<MobileMenuProps> = ({}) => {
  return <div className="lg:hidden">MobileMenu</div>;
};

export default MobileMenu;
